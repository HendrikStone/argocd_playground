apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-backend
spec:
  template:
    spec:
      containers:
      - name: sample-backend
        env:
        - name: key
          value: dev
#        envFrom:
#          - configMapRef:
#              name: app-config
#          - secretRef:
#              name: credentials
